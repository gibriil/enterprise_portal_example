<VirtualHost *:80>
    ServerName localhost

    DocumentRoot "/usr/local/apache2/htdocs"

    <Directory "/usr/local/apache2/htdocs">
        Options FollowSymLinks Includes
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog "/proc/self/fd/1"

    LogLevel warn

    <IfModule log_config_module>
        LogFormat "%a %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\"" combined
        LogFormat "%a %l %u %t \"%r\" %>s %b" common

        <IfModule logio_module>
        LogFormat "%a %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\" %I %O" combinedio
        </IfModule>

        LogFormat "time=%{%Y-%m-%dT%H:%M:%S%z}t level=INFO msg=\"%m Request Received\" method=%m path=%U protocol=%H status=%>s client_ip=%a user_agent=\"%{User-Agent}i\"" goSlogger

        # CustomLog "/proc/self/fd/1" combined
        CustomLog "/proc/self/fd/1" goSlogger
    </IfModule>
</VirtualHost>

<VirtualHost *:8500>
    ServerName localhost

    DocumentRoot "/usr/local/apache2/htdocs/1.25.3"

    <Directory "/usr/local/apache2/htdocs/1.25.3">
        Options FollowSymLinks Includes
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog "/proc/self/fd/1"

    LogLevel warn

    <IfModule log_config_module>
        LogFormat "%a %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\"" combined
        LogFormat "%a %l %u %t \"%r\" %>s %b" common

        <IfModule logio_module>
        LogFormat "%a %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\" %I %O" combinedio
        </IfModule>

        LogFormat "time=%{%Y-%m-%dT%H:%M:%S%z}t level=INFO msg=\"%m Request Received\" method=%m path=%U protocol=%H status=%>s client_ip=%a user_agent=\"%{User-Agent}i\"" goSlogger

        # CustomLog "/proc/self/fd/1" combined
        CustomLog "/proc/self/fd/1" goSlogger
    </IfModule>
</VirtualHost>